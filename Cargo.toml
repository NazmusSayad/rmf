[package]
name = "rmf"
version = "0.1.0"
edition = "2021"
description = "Fast parallel directory deletion tool"

[dependencies]
clap = { version = "4", features = ["derive"] }
walkdir = "2"
rayon = "1"

[profile.release]
opt-level = 3
lto = true
